# Important Notes:
# Every field you add must be added to the root build.gradle expandProps map.

# Project
version=0.2.0
group=cursedflames.modifiers
java_version=21

# Common

# You can find the latest versions here: https://projects.neoforged.net/neoforged/neoforge
# The Minecraft version must agree with the Neo version to get a valid artifact
minecraft_version=1.21.1
mod_name=Modifiers
mod_author=CursedFlames
mod_authors=CursedFlames
mod_id=modifiers
license=MIT
credits=
description=Modifiers for gear (CursedFlames).
# Mod
mod_archives_name=cursedflames-modifiers
mod_version=7.0.8
mod_display_name=CursedFlames-Modifiers
mod_license=MIT

# The Minecraft version range can use any release version of Minecraft as bounds.
# Snapshots, pre-releases, and release candidates are not guaranteed to sort properly
# as they do not follow standard versioning conventions.
minecraft_version_range=[1.21.1]

## You can find a list of all versions here
## https://projects.neoforged.net/neoforged/neoform
# OLD VERSION
#neo_form_version=1.21-20240613.152323
# NEW VERSION - ONLY USED FOR "Vanilla-mode", for common project independent of launcher
#neo_form_version=1.21.1-20240808.144430

# The version of ParchmentMC that is used, see https://parchmentmc.org/docs/getting-started#choose-a-version for new versions
parchment_minecraft=1.21
parchment_version=2024.11.10

## Fabric
fabric_version=0.100.1+1.21
fabric_loader_version=0.15.11

# NeoForge
neoforge_version=21.1.227
neoforge_loader_version_range=[4,)
# loader_version_range=[1,)

# Gradle
org.gradle.jvmargs=-Xmx4G
org.gradle.daemon=false
#org.gradle.daemon=true
#org.gradle.parallel=true
#org.gradle.caching=true
#org.gradle.configuration-cache=true

# Environment Properties

# The Neo version must agree with the Minecraft version to get a valid artifact
neo_version=21.1.227
# The loader version range can only use the major version of FML as bounds
loader_version_range=[1,)


